upstream backend_app{
    server node-app1:3001;
    server node-app2:3002;  
}

server {
    listen 80;

    location / {
        proxy_pass http://backend_app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
